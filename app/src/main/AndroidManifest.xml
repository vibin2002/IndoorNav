<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.bleservertty">

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_miniicon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_miniicon"
        android:supportsRtl="true"
        android:theme="@style/SplashScreenTheme">
        <activity android:name=".auth.SignupActivity">
        </activity>
        <activity android:name=".auth.LoginActivity" >
        </activity>
        <activity android:name=".ble.DisplayDataActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".ble.GattServerActivity">

        </activity>
    </application>

</manifest>